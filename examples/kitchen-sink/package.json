{
  "name": "utopia-kitchen-sink",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "npm run build:client && npm run build:server",
    "build:client": "vite build --outDir dist/client",
    "build:server": "vite build --outDir dist/server --ssr src/entry-server.ts",
    "preview": "NODE_ENV=production node server.js"
  },
  "dependencies": {
    "@utopia/core": "^0.0.1",
    "@utopia/runtime": "^0.0.1",
    "@utopia/router": "^0.0.1",
    "@utopia/server": "^0.0.1",
    "@utopia/email": "^0.0.1",
    "@utopia/ai": "^0.0.1",
    "express": "^4.21.0",
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "@utopia/vite-plugin": "^0.0.1",
    "typescript": "^5.7.0"
  }
}
